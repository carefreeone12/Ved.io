# ============================================================
# Ved.io Python Backend — Runtime Dependencies (Flask)
# Install: pip install -r requirements.txt
# ============================================================

# --- Web framework ---
flask>=3.0.0
flask[async]>=3.0.0          # native async view support
flask-cors>=4.0.0            # CORS middleware
flask-limiter>=3.7.0         # rate limiting
gunicorn>=22.0.0             # production WSGI server

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13.0
aiosqlite>=0.20.0            # SQLite async driver (default for local dev)
asyncpg>=0.29.0              # PostgreSQL async driver (optional for prod)

# --- Config / Validation ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
json-repair>=0.30.0

# --- Logging ---
structlog>=24.1.0

# --- HTTP clients ---
httpx>=0.27.0

# --- File handling ---
aiofiles>=23.2.1
werkzeug>=3.0.0              # used by Flask for file uploads

# --- AI providers (install only what you use) ---
openai>=1.30.0               # needed if using OpenAI or custom endpoint
google-generativeai>=0.7.0   # needed if using Gemini
# huggingface_hub            # optional — HF access via httpx directly

# --- Media / FFmpeg ---
ffmpeg-python>=0.2.0
pillow>=10.3.0

# --- Scheduling & reliability ---
apscheduler>=3.10.4
tenacity>=8.3.0
cachetools>=5.3.3

# --- TTS (free, no API key needed — primary for local prototype) ---
gTTS>=2.5.0

# --- Subtitle support ---
pysrt>=1.1.2

# --- AWS (OPTIONAL — only needed for Polly TTS or S3 storage in production) ---
# boto3>=1.34.0              # Uncomment only if deploying with AWS services

